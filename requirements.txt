# Core
Django==5.2.16
python-dotenv==1.2.2
requests==2.33.0

# AI provider layer (apps.ai.providers): plain HTTP calls to OpenAI/
# Claude/Gemini/Ollama -- no vendor SDKs, see apps/ai/providers/__init__.py
json_repair==0.61.7

# Database / vectors
psycopg[binary]==3.2.10
pgvector==0.4.1

# Frontend glue
django-htmx==1.23.2
Markdown==3.7

# Mail-Ingest: Body als Leitdokument (#1070). beautifulsoup4 entrümpelt den
# (HTML-)Mailbody zu Klartext (Tracking-Pixel/Skripte/zitierte Verläufe raus,
# nur der stdlib-html.parser als Backend -- keine lxml-Binärabhängigkeit).
# fpdf2 rendert daraus ein lesbares PDF für die Ansicht (reines Python, kein
# wkhtmltopdf/Headless-Browser-Binary im Container).
beautifulsoup4==4.13.4
fpdf2==2.8.7

# Cache / queue / background worker (Django-Q2, not Celery)
django-q2==1.8.0
redis==5.2.1
django-redis==5.4.0

# Object storage (S3 / MinIO)
django-storages[s3]==1.14.5
boto3==1.35.99

# MCP service (SSE)
mcp==1.28.1
starlette==1.3.1
uvicorn[standard]==0.34.3

# Extraction cascade (apps.documents.extraction, #1009): text-layer -> OCR
# -> vision. pytesseract/pdf2image are Python bindings only -- the
# tesseract-ocr + poppler-utils binaries they shell out to are installed
# in the Dockerfile, not via pip.
pypdf==6.15.0
pdf2image==1.17.0
pytesseract==0.3.13
python-docx==1.2.0
Pillow==12.3.0
langdetect==1.0.9
# Thumbnails (apps.documents.thumbnails, #1123): erste PDF-Seite fuer die
# Kachelansicht rastern. pypdfium2 buendelt die PDFium-Engine als reines
# Python-Wheel -- bewusst kein pdf2image/Poppler-Weg hier, damit die
# Thumbnail-Erzeugung nicht an der tesseract/poppler-Systeminstallation
# haengt; Bilder skaliert Pillow (schon oben).
pypdfium2==4.30.0
# Robuste MIME-Erkennung aus dem Dateiinhalt (#1077): Scanner/Upload-Wege
# melden fuer echte PDFs oft `application/octet-stream`. python-magic bindet
# die auf dem Server vorhandene libmagic (`file`) an; fehlt sie, faellt
# apps.documents.mime auf Magic-Bytes + Dateiendung zurueck.
python-magic==0.4.27

# Production server
gunicorn==23.0.0

# Error monitoring (config.settings.base): DjangoIntegration reports unhandled
# exceptions/Django errors when SENTRY_DSN is set, otherwise never initialised.
sentry-sdk==2.66.1
whitenoise
