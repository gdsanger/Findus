# Core
Django==5.2.15
python-dotenv==1.2.2
requests==2.31.0

# AI provider layer (apps.ai.providers): plain HTTP calls to OpenAI/
# Claude/Gemini/Ollama -- no vendor SDKs, see apps/ai/providers/__init__.py

# Database / vectors
psycopg[binary]==3.2.10
pgvector==0.4.1

# Frontend glue
django-htmx==1.23.2

# Cache / queue / background worker (Django-Q2, not Celery)
django-q2==1.8.0
redis==5.2.1
django-redis==5.4.0

# Object storage (S3 / MinIO)
django-storages[s3]==1.14.5
boto3==1.35.99

# MCP service (SSE)
mcp==1.28.1
starlette==1.3.1
uvicorn[standard]==0.34.3

# Extraction cascade (apps.documents.extraction, #1009): text-layer -> OCR
# -> vision. pytesseract/pdf2image are Python bindings only -- the
# tesseract-ocr + poppler-utils binaries they shell out to are installed
# in the Dockerfile, not via pip.
pypdf==6.14.2
pdf2image==1.17.0
pytesseract==0.3.13
python-docx==1.2.0
Pillow==12.3.0
langdetect==1.0.9

# Production server
gunicorn==23.0.0
